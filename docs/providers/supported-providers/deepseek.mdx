---
title: "DeepSeek"
description: "DeepSeek API conversion guide - Anthropic-compatible chat and responses, OpenAI-compatible beta text completions, streaming, tool calling, and reasoning"
icon: "d"
---

## Overview

DeepSeek is a provider with a dedicated Bifrost provider implementation that mixes two upstream API layouts: Chat Completions and the Responses API are sent to DeepSeek's **Anthropic-compatible** endpoint, while Text Completions still use DeepSeek's **OpenAI-compatible** beta FIM endpoint. Key characteristics:

- **Anthropic-compatible chat** - Chat Completions use `/anthropic/v1/messages`, authenticated with `x-api-key`
- **Anthropic-compatible Responses API** - Responses requests are sent natively to `/anthropic/v1/messages` (no internal conversion to Chat Completions)
- **Streaming support** - Server-Sent Events for chat, responses, and text completions
- **Tool calling** - Function tools are converted using the shared Anthropic request/response converters
- **Reasoning support** - Reasoning/thinking parameters are mapped through the Anthropic converters
- **Beta text completions** - Text/FIM completions still use DeepSeek's OpenAI-compatible `/beta/completions` endpoint

### Supported Operations

| Operation | Non-Streaming | Streaming | Endpoint |
|-----------|---------------|-----------|----------|
| Chat Completions | ✅ | ✅ | `/anthropic/v1/messages` |
| Responses API | ✅ | ✅ | `/anthropic/v1/messages` |
| Text Completions | ✅ | ✅ | `/beta/completions` |
| List Models | ✅ | - | `/models` |
| Embeddings | ❌ | ❌ | - |
| Image Generation | ❌ | ❌ | - |
| Speech (TTS) | ❌ | ❌ | - |
| Transcriptions (STT) | ❌ | ❌ | - |
| Files | ❌ | ❌ | - |
| Batch | ❌ | ❌ | - |

<Note>
**Unsupported Operations** (❌): Embeddings, Image Generation, Speech, Transcriptions, Files, Batch, cached content, containers, token counting, compaction, OCR, rerank, video, and passthrough are not supported by the upstream DeepSeek API through this provider. These return `UnsupportedOperationError`.
</Note>

## Setup & Configuration

Configure DeepSeek as a provider.

<Tabs>
<Tab title="Web UI">

![DeepSeek provider dashboard](../../media/provider-dashboard-deepseek.png)

1. Navigate to **Models** > **Model Providers**. Look for **DeepSeek** under **Configured Providers**. If it is missing, click on **Add New Provider** and select **DeepSeek**.
2. Click **Add Key** or edit an existing key.
3. Set a name for your key.
4. Paste your API key directly or use an environment variable (for example, `env.DEEPSEEK_API_KEY`).
5. Set **Allowed Models** to **All Models** (default) or the specific model allowlist you want this key to serve.
6. Save the provider configuration.

</Tab>
<Tab title="config.json">

```json
{
  "providers": {
    "deepseek": {
      "keys": [
        {
          "name": "deepseek-key-1",
          "value": "env.DEEPSEEK_API_KEY",
          "models": [
            "*"
          ],
          "weight": 1.0
        }
      ]
    }
  }
}
```

</Tab>
<Tab title="API">
Refer to the API documentation for [Provider Keys Management](https://docs.getbifrost.ai/api-reference/providers/create-a-key-for-a-provider).
</Tab>
<Tab title="Go SDK">

```go
case schemas.DeepSeek:
    return []schemas.Key{{
        Name:   "deepseek-key-1",
        Value:  *schemas.NewSecretVar("env.DEEPSEEK_API_KEY"),
        Models: []string{"*"},
        Weight: 1.0,
    }}, nil
```

</Tab>
</Tabs>

---

# 1. Chat Completions

## Request Parameters

DeepSeek Chat Completions are sent to DeepSeek's Anthropic-compatible endpoint (`/anthropic/v1/messages`), authenticated with the `x-api-key` header instead of a bearer token. Requests and responses are built using the shared Anthropic converters. For the full parameter reference and message conversion behavior, see [Anthropic Chat Completions](/providers/supported-providers/anthropic#1-chat-completions).

### Authentication

Unlike DeepSeek's OpenAI-compatible endpoints (which use `Authorization: Bearer`), the Anthropic-compatible endpoint is authenticated with `x-api-key: <key>`. Bifrost sets this header automatically from the configured provider key.

### Reasoning Parameter

Reasoning/thinking parameters are mapped through the Anthropic converters (`reasoning` → `thinking`), the same as the [Anthropic provider](/providers/supported-providers/anthropic#1-chat-completions). Reasoning effort is sent as `output_config.effort` (Anthropic's own field placement), not nested under `thinking.reasoning_effort` as DeepSeek's native API documents it.

### Forced Tool Choice

DeepSeek's backend rejects a forced `tool_choice` (a specific named function) while thinking is enabled, with `"Thinking mode does not support this tool_choice"` — and DeepSeek models run with thinking enabled by default even when no `reasoning` parameter is set. Bifrost automatically disables thinking (`thinking: {"type": "disabled"}`) whenever `tool_choice` pins a specific function, for both Chat Completions and the Responses API. `tool_choice: "required"`/`"any"` (forcing some tool call, not a specific one) is left untouched, since DeepSeek accepts that combination with thinking on.

### Extra Parameters

Provider-specific options can still be sent through `extra_params`. This is currently the only way to reach a few DeepSeek-documented parameters that have no native mapping through the Anthropic converters: `response_format: {"type": "json_object"}` (only `json_schema` is natively converted), `logprobs`/`top_logprobs` (no equivalent field exists on the Anthropic-compatible request shape), and `reasoning_content` (beta Chat Prefix Completion).

DeepSeek supports Anthropic-style message types, tools, responses, and streaming formats via `/anthropic/v1/messages`. For details on message handling, tool conversion, responses, and streaming, refer to [Anthropic Chat Completions](/providers/supported-providers/anthropic#1-chat-completions).

---

# 2. Responses API

Responses requests are sent natively to DeepSeek's Anthropic-compatible endpoint at `/anthropic/v1/messages` — there is no internal conversion to Chat Completions. Both non-streaming and streaming Responses requests build an Anthropic-format request body directly from the `BifrostResponsesRequest` and convert the response back to Bifrost's Responses format.

Same parameter support as Chat Completions with response format differences (output items instead of message content).

---

# 3. Text Completions

DeepSeek supports beta text/FIM (Fill-In-Middle) completions through `/beta/completions`:

| Parameter | Mapping |
|-----------|---------|
| `prompt` | Sent as-is |
| `suffix` | Enables FIM mode — text that should follow the completion; sent as-is |
| `max_tokens` | max_tokens |
| `temperature` | temperature |
| `top_p` | top_p |
| `stop` | stop sequences |
| `echo` | echo |
| `logprobs` | logprobs |
| `extra_params` | Passed through to DeepSeek (e.g. `thinking` control) |

Setting `suffix` alongside `prompt` puts the request in FIM mode: DeepSeek generates the text that belongs between `prompt` and `suffix` rather than a plain continuation of `prompt`.

Response returns `choices[].text` with completion text.

---

# 4. Text Completions Streaming

Streaming text completions use DeepSeek's OpenAI-compatible SSE format on `/beta/completions`.

---

# 5. List Models

Lists available models from DeepSeek through `/models`.

---

## Unsupported Features

| Feature | Reason |
|---------|--------|
| Embedding | Not offered by DeepSeek API through this provider |
| Image Generation | Not offered by DeepSeek API through this provider |
| Speech/TTS | Not offered by DeepSeek API through this provider |
| Transcription/STT | Not offered by DeepSeek API through this provider |
| Batch Operations | Not offered by DeepSeek API through this provider |
| File Management | Not offered by DeepSeek API through this provider |
| Cached Content | Only Gemini and Vertex AI support cached content in Bifrost |
| Container Management | Not offered by DeepSeek API through this provider |
| Token Counting | Not offered by DeepSeek API through this provider |
| Rerank/OCR/Video | Not offered by DeepSeek API through this provider |

---

## Caveats

<Accordion title="Default Base URL">
**Severity**: Low
**Behavior**: DeepSeek defaults to `https://api.deepseek.com`
**Impact**: Custom DeepSeek-compatible deployments must override `network_config.base_url`
**Code**: `NewDeepSeekProvider` sets the default base URL when no provider-level base URL is configured
</Accordion>

<Accordion title="Beta Text Completion Endpoint">
**Severity**: Medium
**Behavior**: Text completions are routed to `/beta/completions`
**Impact**: FIM/text completion behavior follows DeepSeek's beta API contract and may differ from standard OpenAI `/completions`
**Code**: `TextCompletion` and `TextCompletionStream` use `/beta/completions`
</Accordion>

<Accordion title="User Field Size Limit (Text Completions)">
**Severity**: Low
**Behavior**: User field > 64 characters is silently dropped
**Impact**: Longer user identifiers are lost
**Code**: `SanitizeUserField` enforces 64-char max in the shared OpenAI converter, still used for Text Completions
</Accordion>

<Accordion title="Forced Tool Choice Disables Thinking">
**Severity**: Medium
**Behavior**: When `tool_choice` pins a specific named function, Bifrost forces `thinking: {"type": "disabled"}` in the outbound request, overriding any `reasoning` parameter
**Impact**: DeepSeek's Anthropic-compatible endpoint returns `"Thinking mode does not support this tool_choice"` for a forced tool call while thinking is on (the default); this override prevents that error. `tool_choice: "required"`/`"any"` is unaffected and keeps thinking on.
**Code**: `core/providers/anthropic/chat.go` and `core/providers/anthropic/responses.go`, gated on `Provider == DeepSeek`
</Accordion>

<Accordion title="Mixed Endpoint Compatibility">
**Severity**: Medium
**Behavior**: Chat Completions and the Responses API are sent to DeepSeek's Anthropic-compatible endpoint (`/anthropic/v1/messages`, authenticated with `x-api-key`), while Text Completions still use the OpenAI-compatible beta endpoint (`/beta/completions`, authenticated with a bearer token)
**Impact**: The two operation families follow different upstream contracts; parameters and behavior documented for the [OpenAI provider](/providers/supported-providers/openai) no longer apply to DeepSeek Chat Completions or Responses — refer to the [Anthropic provider](/providers/supported-providers/anthropic) docs for those instead
**Code**: `ChatCompletion`, `ChatCompletionStream`, `Responses`, and `ResponsesStream` in `core/providers/deepseek/deepseek.go` delegate to the `anthropic` package; `TextCompletion` and `TextCompletionStream` still delegate to the `openai` package
</Accordion>